# getrandom 0.3 requires opting into its JS backend on wasm32-unknown-unknown
# (used by typesec-wasm via jsonschema→ahash). Host builds are unaffected.
[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']
